package reconcile

// Input is the reconcile request. A pass takes no external parameters; the type
// fixes the use-case signature and reserves room for future validated options.
type Input struct{}
